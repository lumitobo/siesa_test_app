name: siesa_app
description: "App test for siesa"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.2

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.0.3
  cupertino_icons: ^1.0.8
  flutter_dotenv: ^5.0.2
  flutter_riverpod: ^2.3.2
  flutter_staggered_grid_view: ^0.7.0
  formz: ^0.8.0
  go_router: ^14.6.0
  google_fonts: ^6.2.1
  shared_preferences: ^2.0.18
  intl: ^0.18.0
  supabase_flutter: ^2.8.1
  image_picker: ^0.8.7

dev_dependencies:
  flutter_test:
    sdk: flutter


  flutter_lints: ^4.0.0

flutter:

  uses-material-design: true

  assets:
    - .env
    - assets/loaders/
    - assets/images/
